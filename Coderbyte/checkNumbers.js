function CheckNums(num1,num2) { 

    if (num2 > num1){ //checks if number 2 is larger than number 1
    return "true";
    
      }else if ( num1 > num2){ //checks if number 1 is larger than number 2
        return "false";
      }else{ // returns -1 if both are equal
        return "-1";
        
      }
         
}