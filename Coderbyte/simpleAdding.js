function SimpleAdding(num) {
  var total = 0;				// make a new variable starting at 0
  for (var i = 0; i <= num; i++) {  // loops through numbers 
    total += i;                  // adds them all together
}
  return total; 				// returns the correct value
}